import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  logoContainer: {
    width: '100%',
    display: 'flex',
    justifyContent: 'center',
    alignContent: 'center',
  },
  logo: {
    width: '100%',
    height: '100%',
    minWidth: '272px',
    maxWidth: '456px',
  },
}));
